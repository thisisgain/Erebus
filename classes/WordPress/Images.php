<?php

namespace Origin\WordPress;

class Images {
    
    public function __construct()
	{
        // ── IMAGE SIZES ───────────────────────────────────────────────────────
        // Register only the sizes your design system uses.
        // Unregistered sizes waste server storage and processing time.
        /*
        add_image_size( 'card-thumbnail',    480, 320, true );   // 3:2 card image
        add_image_size( 'card-thumbnail-2x', 960, 640, true );   // Retina
        add_image_size( 'hero-full',         1920, 1080, true );  // Full-bleed hero
        add_image_size( 'hero-medium',       1200, 675, true );   // Medium hero
        add_image_size( 'square-sm',         200, 200, true );    // Avatar/icon
        add_image_size( 'og-image',          1200, 630, true );   // Open Graph
        */
    }
    
}